<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User: <%= listing.title %></title>
    <link
      rel="shortcut icon"
      href="https://cdn-icons-png.flaticon.com/128/13225/13225806.png"
      type="image/x-icon"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      .card {
        border-radius: 15px;
        background-color: #343a40;
        color: white;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      }

      .card img {
        border-radius: 15px 15px 0 0;
        height: 400px;
        object-fit: cover;
      }

      .card-title {
        font-size: 1.8rem;
      }

      .card-body p {
        font-size: 1.1rem;
      }

      .edit {
        margin-top: 30px;
        text-align: center;
      }

  
    </style>
  </head>
  <body>
    <% layout("/layouts/boilerplate.ejs") -%>
    <div class="container">
      <h3 class="text-center mt-5">Details of: <%= listing._id %></h3>

      <div class="row justify-content-center mt-4" style="cursor: pointer;">
        <div class="col-md-8">
          <div class="card">
            <img
              class="card-img-top"
              src="<%= listing.image %>"
              alt="<%= listing.title %>"
            />
            <div class="card-body text-center">
              <h5 class="card-title font-weight-bold">
                Title: <%= listing.title %>
              </h5>
              <p class="card-text">
                <strong>Description: </strong><%= listing.description %>
              </p>
              <p class="card-text">
                <strong>Price: &#2352;&#2370;<%= listing.price %></strong>
              </p>
              <p class="card-text">
                <strong>Location: </strong><%= listing.location %>
              </p>
              <p class="card-text">
                <strong>Country: </strong><%= listing.country %>
              </p>
            </div>
          </div>
        </div>
      </div>

      <div class="edit">
        <a href="/listings/<%= listing._id %>/edit" class="btn btn-primary edit-btn">
          Edit your Listings
        </a>

        <form
          action="/listings/<%= listing._id %>/?_method=DELETE"
          method="POST"
        >
        <br><br>
          <button type="submit" class="btn btn-success mx-auto">Delete</button>
        </form>
        <br><br> 

      </div>

      <hr/>
      <div class="col-8 offest-3 mb-3">
        <h3>Leave a Rating</h3>

        <form action="/listings/<%= listing.id %>/reviews" method="post" class="needs-validation" novalidate>
          
            <div class="mb-3 mt-3">
              <label for="range" class="form-label">Your Rating</label>
              <input type="range" id="range" min="1" max="5" name="review[rating]" class="form-range">
            </div>
            <hr/>
            <div class="mb-3 mt-3">
                <label for="Comments">Your Comments:</label>
                <textarea name="review[comment]" id="comments" cols="30" rows="5" placeholder="Please tell me your experience?"  class="form-control" required></textarea>
                <div class="invalid-feedback">Please Write on your Feedback.</div>
            </div>
            <br><br>
            <button class="btn btn-primary">Submit</button>
        </form>
      </div>
      
      <!-- <p><%= listing.reviews %></p> -->
      <!-- <ul>
        <% for(review of listing.reviews){%>
        <p><%=review.rating %>Stars</p>
        <p><%= review.comment %></p>
        <%}%>
      </ul> -->
      
      <!-- for the giving the card stylng of our reviews. -->
      <h2><b>All Reviews:</b></h2>
      <div class="row">
        <% for(review of listing.reviews){ %>
          <div class="col-md-6 mb-3">
            <div class="card">
              <div class="card-body">
                <div class="card-title">Name:John Doe</div>
                <div class="card-text">Comment: <%= review.comment %></div>
                <div class="card text">Rating: <%= review.rating %></div>
                <div class="card-text"><%=review.createdAt %></div>
                <form action="/listings/<%= listing._id %>/reviews/<%=review._id %>?_method=DELETE" method="post" class="mb-3 mt-0">
                
                  <button class="btn btn-sm btn-dark">Delete</button>
                </form>
              </div>
            </div>
          </div>
        <%} %>
      </div>
<script src="/js/script.js"></script>

  </body>
</html>
